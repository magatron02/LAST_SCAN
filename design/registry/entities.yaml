# Game World Entity Registry
#
# PURPOSE: Single source of truth for all named game-world facts that appear
# in more than one document. Skills check this before writing new content to
# detect cross-doc inconsistencies at authoring time.
#
# RULES:
#   - Only register facts that cross system boundaries. Internal-only facts
#     (a formula used in one GDD only) do NOT need to be registered.
#   - Never delete entries — set status: deprecated instead.
#   - When a value changes: update the value, set revised: to today's date,
#     and add a comment with the old value and which GDD changed it.
#   - source: is the authoritative GDD — the one that "owns" this fact.
#     Other GDDs that reference it list themselves in referenced_by.
#   - When a new GDD references an existing entry, append its path to
#     referenced_by. Do not create a duplicate entry.
#
# WRITTEN BY: /design-system (Phase 5 — after GDD sections are approved)
#             /consistency-check (when resolving conflicts)
# READ BY:    /design-system (Phase 2 — before authoring begins, and after
#               Section C/D writes to conflict-check new entities/formulas)
#             /consistency-check (primary input — grep-first, GDD-second)
#             /review-all-gdds (Phase 1 — baseline for Phase 2 checks)
#             /architecture-review (data structure and interface validation)
#
# SEARCH PATTERNS (for skills using Grep):
#   All entity names:        Grep pattern="^  - name:" path="design/registry/entities.yaml"
#   Specific entity:         Grep pattern="  - name: goblin" path="design/registry/entities.yaml"
#   All items:               Grep pattern="^  - name:" path="design/registry/entities.yaml" (items section)
#   What combat.md owns:     Grep pattern="source: design/gdd/combat.md"
#   What inventory.md uses:  Grep pattern="referenced_by.*inventory"
#   All gold values:          Grep pattern="value_gold:"
#   Deprecated entries:      Grep pattern="status: deprecated"
#
# FORMAT: YAML. Consistent indentation is critical for grep reliability.
#   Sections: entities | items | events | formulas | constants
#   Each entry: name, status, source, referenced_by[], attributes{}, added, revised

version: 1
last_updated: "2026-07-02"  # Orchestrator #5 round-3 revision: scan:integrity_* "{...}" placeholders replaced with declared field sets; session:tick elapsedSeconds pinned as capped-dt game time; dt_cap constant registered (AC-OR01/AC-OR34)

# ─── ENTITIES ────────────────────────────────────────────────────────────────
# Named game-world objects: enemies, NPCs, characters, factions, bosses.
# Register an entity here when it appears in more than one GDD — e.g., an
# enemy defined in combat.md that also drops items listed in inventory.md.
#
# Required fields: name, status, source, referenced_by, added
# Attribute fields: any key stats that could appear in another GDD
#   (health, damage, drops, faction, etc.)

entities: []

# Example (remove when first real entry is added):
#
# entities:
#   - name: goblin
#     status: active              # active | deprecated
#     source: design/gdd/combat.md
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/inventory.md
#     health: 40
#     damage: 8
#     drops:
#       - item: goblin_arm        # must match an entry in items section
#         qty: 1
#         drop_rate: 0.8          # 0.0–1.0
#     added: 2026-03-26
#     revised: ""


# ─── ITEMS ───────────────────────────────────────────────────────────────────
# Named collectables, equipment, consumables, crafting materials, currency.
# Register an item when its name or value appears in more than one GDD.
#
# Required fields: name, status, source, referenced_by, added
# Attribute fields: value_gold, weight, stackable, category, and any stat
#   modifiers that another system (e.g., economy, crafting) might reference.

items: []

# Example:
#
# items:
#   - name: goblin_arm
#     status: active
#     source: design/gdd/combat.md   # drop defined in combat GDD
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/inventory.md    # inventory GDD lists its weight/stack rules
#       - design/gdd/economy.md      # economy GDD references its sell price
#     value_gold: 5
#     weight: 1
#     stackable: true
#     category: crafting_material
#     added: 2026-03-26
#     revised: ""


# ─── EVENTS ──────────────────────────────────────────────────────────────────
# Cross-system events on the Orchestrator bus. The Orchestrator GDD (#5) is the
# authoritative registry: no system may publish an event under a name this
# section does not own, and no system may consume an event whose payload shape
# contradicts what its producer declares here.
#
# Required fields: name, status, source (producer GDD), registered_by, consumers,
#   payload, kind (latest-value | discrete), added
#   - kind: latest-value = Orchestrator caches most recent, replays to late
#     subscribers. discrete = fire-and-forget, never replayed (except session:end).
#   - status: active = producer designed & approved. provisional = producer GDD
#     not yet authored (shape inferred from consumers; producer must confirm).

events:
  # -- Orchestrator-owned (nothing produced these before #5) --
  - name: session:tick
    status: active
    source: design/gdd/orchestrator.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md]  # session_escalation elapsed-time input; + future timed systems
    payload: "{elapsedSeconds}"  # capped-dt GAME TIME, not wall-clock: per-frame delta clamped to dt_cap (0.1s) — tab-restore cannot jump it (Orchestrator Rule 3, AC-OR34)
    kind: latest-value
    added: 2026-07-01
    revised: "2026-07-02"
  - name: session:end
    status: active
    source: design/gdd/orchestrator.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md, design/gdd/scan-node-system.md]  # + Win/Lose #10
    payload: "{} (transition marker)"
    kind: discrete  # SOLE cached exception — delivered once to a subscriber joining during SEALED
    added: 2026-07-01
  - name: session:request_end
    status: active
    source: design/gdd/orchestrator.md  # intake; Orchestrator alone flips state + emits session:end
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/orchestrator.md]  # produced by any system; Win/Lose #10 primary caller
    payload: "{reason}"
    kind: discrete
    added: 2026-07-01
  # -- player / movement --
  - name: player:position
    status: active
    source: design/gdd/fps-movement.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md]  # + Entity #9, UI/HUD #12
    payload: "{x, y, z}"
    kind: latest-value
    added: 2026-07-01
  - name: movement:scan_triggered
    status: provisional  # producer Scan Mechanic #8 undesigned
    source: design/gdd/scan-mechanic.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/fps-movement.md]
    payload: "{nodePosition}"
    kind: discrete
    added: 2026-07-01
  # -- scan family --
  - name: scan:coverage
    status: active
    source: design/gdd/scan-node-system.md  # canonical over scan:complete.coverage (Scan Node Cross-System Invariants)
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md]  # session_escalation coverage input; + Win/Lose, UI/HUD
    payload: "{coverage}  # level 0.0-1.0, not delta"
    kind: latest-value
    added: 2026-07-01
  - name: scan:complete
    status: active
    source: design/gdd/scan-node-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md, design/gdd/point-cloud-renderer.md, design/gdd/fps-movement.md]
    payload: "{nodeId, valid, entityCaptured, coverage}"  # verbatim from Scan Node AC-SN05/SN21 (was "{nodeId, coverage, ...}" — under-specified)
    kind: discrete
    added: 2026-07-01
  - name: scan:abort
    status: active
    source: design/gdd/scan-node-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/point-cloud-renderer.md, design/gdd/fps-movement.md]
    payload: "{nodeId, coverage}"  # verbatim from Scan Node AC-SN06 (was "{nodeId}" — dropped coverage)
    kind: discrete  # scan:complete takes priority if both queued same tick (Point Cloud AC-E02)
    added: 2026-07-01
  - name: scan:started
    status: provisional  # producer Scan Mechanic #8 undesigned
    source: design/gdd/scan-mechanic.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/scan-node-system.md]
    payload: "{nodeId}"
    kind: discrete
    added: 2026-07-01
  - name: scan:captured
    status: provisional  # producer Scan Mechanic #8 undesigned
    source: design/gdd/scan-mechanic.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/scan-node-system.md]
    payload: "{nodeId, valid, entityInFrame}"
    kind: discrete
    added: 2026-07-01
  - name: scan:capture_frame
    status: provisional  # producer Scan Mechanic #8 undesigned
    source: design/gdd/scan-mechanic.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/point-cloud-renderer.md]
    payload: "{angle, progress}"
    kind: discrete
    added: 2026-07-01
  - name: scan:integrity_warning
    status: active
    source: design/gdd/scan-node-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/floor-plan-system.md]  # + UI/HUD #12
    payload: "{count}"  # verbatim from Scan Node Core Rule 8 / Interactions table (was "{...}" placeholder — not a comparable field set, round-3 blocker)
    kind: discrete
    added: 2026-07-01
    revised: "2026-07-02"
  - name: scan:integrity_failure
    status: active
    source: design/gdd/scan-node-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: []  # Win/Lose #10, UI/HUD #12 (both undesigned)
    payload: "{}"  # payload-less transition marker — now declared explicitly in Scan Node Interactions table (was "{...}" placeholder, round-3 blocker)
    kind: discrete
    added: 2026-07-01
    revised: "2026-07-02"
  # -- floorplan family --
  - name: floorplan:init
    status: active
    source: design/gdd/floor-plan-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/scan-node-system.md]  # builds node roster + coverage denominator S from this
    payload: "{propertyId, roomMeta:[{id,type}], nodeRoster:[{nodeId,nodeType,roomId,estimatedPosition}]}"  # verbatim from Floor Plan Core Rules §; complete roster incl. hidden ANOMALY_FINAL/NULL (was "{roster, roomMapping}" — renamed fields, dropped propertyId)
    kind: latest-value  # one-time; 2nd fire REJECTED, cached roster never changes (Orchestrator AC-OR21)
    added: 2026-07-01
  - name: floorplan:update
    status: active
    source: design/gdd/floor-plan-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/point-cloud-renderer.md, design/gdd/scan-node-system.md, design/gdd/fps-movement.md]
    payload: "{rooms:[{id, aabb, surfaces}]}  # renderable geometry only; one emission per mutation (AC-C06)"
    kind: discrete
    added: 2026-07-01
  - name: floorplan:reveal
    status: active
    source: design/gdd/floor-plan-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/scan-node-system.md]
    payload: "{roomId}"
    kind: discrete
    added: 2026-07-01
  - name: floorplan:loop
    status: active
    source: design/gdd/floor-plan-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/fps-movement.md]  # applies the teleport reposition
    payload: "{targetPosition, targetYaw, toRoom}"
    kind: discrete  # delivered AFTER floorplan:update when both same tick (Floor Plan AC-L09)
    added: 2026-07-01
  # -- entity / renderer --
  - name: entity:proximity
    status: provisional  # producer Entity #9 undesigned; tier vocab (4 vs 5) is Entity's decision
    source: design/gdd/entity-system.md
    registered_by: design/gdd/orchestrator.md
    consumers: [design/gdd/point-cloud-renderer.md, design/gdd/floor-plan-system.md]  # + Audio #6, UI/HUD #12
    payload: "{tier}"
    kind: latest-value
    added: 2026-07-01
  - name: renderer:anomaly_density
    status: active
    source: design/gdd/point-cloud-renderer.md
    registered_by: design/gdd/orchestrator.md
    consumers: []  # UI/HUD #12 (undesigned)
    payload: "{type, sigma}"
    kind: discrete
    added: 2026-07-01


# ─── FORMULAS ────────────────────────────────────────────────────────────────
# Named calculations with defined variables and output ranges.
# Register a formula when its output feeds into another system's input,
# or when another GDD references it by name.
#
# Required fields: name, status, source, referenced_by, variables[],
#   output_range[min, max], added
# Optional: expression (the actual formula), notes

formulas:
  - name: session_escalation
    status: active
    source: design/gdd/floor-plan-system.md
    referenced_by:
      - design/gdd/floor-plan-system.md
      # Reads coverage from Scan Node System; drives perception-stripping intensity
    variables:
      - t
      - T_session
      - coverage
      - w_t
      - w_c
    output_range: [0, 1]
    expression: "clamp(w_t*(t/T_session) + w_c*coverage, 0, 1)"
    notes: "Single 0-1 escalation dial. coverage is owned by Scan Node System
      (consumed via Orchestrator). Feeds desync_delay. w_t capped at 0.8 (w_c >= 0.2)
      as of 2026-06-30 re-review -- without the cap, w_t=1.0 let e reach 1.0 from
      elapsed time alone, breaking the desync_delay D_max-only-via-trap guarantee."
    added: 2026-06-27
    revised: "2026-06-30"

  - name: coverage
    status: active
    source: design/gdd/scan-node-system.md
    referenced_by:
      - design/gdd/scan-node-system.md
      - design/gdd/floor-plan-system.md  # session_escalation reads coverage as input
      # Win/Lose GDD (when authored): inverted-reward ending evaluation (§9)
    variables:
      - V
      - S
    output_range: [0.0, 1.0]
    expression: "coverage = V / S   (V = VALID nodes; S = count(STANDARD) + 1 anomaly node; NULL excluded; S fixed at init)"
    notes: "Single coverage fraction. Denominator includes the ANOMALY_FINAL node
      from init, so escape (all standard, anomaly unscanned) tops out at N/(N+1) <
      1.0; 1.0 reachable only by scanning the anomaly node (Completion Trap §9).
      UI nodesCompleted counter uses a DIFFERENT (standard-only) denominator — do
      not conflate."
    added: 2026-06-27
    revised: ""

  - name: desync_delay
    status: active
    source: design/gdd/floor-plan-system.md
    referenced_by:
      - design/gdd/floor-plan-system.md
      # Governs dollhouse mask lag -> consumed by UI/HUD dollhouse panel
    variables:
      - e
      - D0
      - D_max
    output_range: [0.0, 120.0]  # full safe config envelope; defaults produce 2.0–25.0s, escape ceiling ~22.5s
    expression: "D0 + (D_max - D0) * e^3"  # was e^2; cubic since re-review 2026-06-27 (floor-plan design-review)
    notes: "Seconds the dollhouse lags reality (continuous player-position history +
      discrete scan-state queue). e from session_escalation. CUBIC so map stays truthful
      early, degrades steeply late (perception stripping). D_max is reached only via
      Completion Trap AT ANY LEGAL CONFIG (structurally guaranteed since 2026-06-30 by
      the session_escalation w_t<=0.8 cap -- prior to that the claim only held at
      default weights). D0 < D_max enforced at load since 2026-06-30 (D0=D_max collapses
      the curve to a flat constant). 0-120 is the allowed cross-configuration envelope."
    added: 2026-06-27
    revised: "2026-06-30"

# Example:
#
# formulas:
#   - name: damage_formula
#     status: active
#     source: design/gdd/combat.md
#     referenced_by:
#       - design/gdd/combat.md
#       - design/gdd/progression.md   # progression GDD scales attack variable
#     variables:
#       - attack
#       - defense
#       - crit_chance
#       - crit_multiplier
#     output_range: [0, 999]
#     expression: "max(0, attack - defense) * (1 + crit_chance * crit_multiplier)"
#     notes: "Output feeds into health-system damage intake. Min is 0 (armour
#       can absorb all damage). Max is uncapped in formula but tuning knob
#       damage_cap in combat.md clamps at 999."
#     added: 2026-03-26
#     revised: ""


# ─── CONSTANTS ───────────────────────────────────────────────────────────────
# Named numerical values referenced across multiple systems.
# Register a constant when it is defined in one GDD but another GDD must
# agree with it (e.g., gold carry limit defined in economy but checked in
# inventory, or base inventory slots defined in inventory but displayed in HUD).
#
# Required fields: name, status, source, referenced_by, value, unit, added

constants:
  - name: MOVE_SPEED
    status: active
    source: design/gdd/fps-movement.md
    referenced_by:
      - design/gdd/fps-movement.md
      # Entity System GDD: entity chase speed defined relative to this
    value: 1.6
    unit: m/s
    added: 2026-06-26
    revised: ""

  - name: EYE_HEIGHT
    status: active
    source: design/gdd/fps-movement.md
    referenced_by:
      - design/gdd/fps-movement.md
      - design/gdd/floor-plan-system.md  # loopSpawn clamp height on loop teleport
      # Point Cloud Renderer: camera Y = EYE_HEIGHT (renderer origin height)
    value: 1.5
    unit: metres
    added: 2026-06-26
    revised: ""

  - name: WALL_MARGIN
    status: active
    source: design/gdd/fps-movement.md
    referenced_by:
      - design/gdd/fps-movement.md
      - design/gdd/floor-plan-system.md  # door min width 0.71m = 2 × WALL_MARGIN + 0.01
      # Level designer checklist: min corridor width = 2 × WALL_MARGIN + 0.01 = 0.71m
    value: 0.35
    unit: metres
    added: 2026-06-26
    revised: ""

  - name: dt_cap
    status: active
    source: design/gdd/fps-movement.md  # Formula 1's "hard cap at 0.1s" — named here because a second system now shares it
    referenced_by:
      - design/gdd/fps-movement.md
      - design/gdd/orchestrator.md  # session:tick elapsedSeconds accumulates the same capped delta (Rule 3, AC-OR34)
    value: 0.1
    unit: seconds
    added: 2026-07-02
    revised: ""

  - name: MOUSE_SENSITIVITY
    status: active
    source: design/gdd/fps-movement.md
    referenced_by:
      - design/gdd/fps-movement.md
      # UI/HUD: settings menu sensitivity slider
    value: 0.0010
    unit: rad/px
    added: 2026-06-26
    revised: ""

  - name: PITCH_LIMITS
    status: active
    source: design/gdd/fps-movement.md
    referenced_by:
      - design/gdd/fps-movement.md
      # Scan Mechanic: scan capture arc operates within these limits in NAVIGATE
    value: "min=-1.3963, max=+1.3963"
    unit: radians
    added: 2026-06-26
    revised: ""

  - name: base_density
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
    value: 900
    unit: pts/m²
    added: 2026-06-26
    revised: ""

  - name: density_budget_ceiling
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
    value: 1500000
    unit: points
    added: 2026-06-26
    revised: ""

  - name: anomaly_sigma_threshold
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
      # UI/HUD GDD will reference this (subscribes to renderer:anomaly_density events)
    value: 2.5
    unit: sigma
    added: 2026-06-26
    revised: ""

  - name: scan_frame_duration
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
      # Scan Mechanic GDD will reference this (total scan cycle = 4 × T)
    value: 0.5
    unit: seconds
    added: 2026-06-26
    revised: ""

  - name: proximity_tier_near
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
      - design/gdd/floor-plan-system.md  # loop_trigger_tier default = NEAR
      # Entity System GDD will reference these distance bands
    value: "d_min=3.0, d_max=8.0, J_max=0.020"
    unit: metres / metres-per-frame
    added: 2026-06-26
    revised: ""

  - name: entity_influence_radius
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
      # Entity System GDD will reference this (how far entity visual effects extend)
    value: 5.0
    unit: metres
    added: 2026-06-26
    revised: ""

  - name: proximity_tier_adjacent
    status: active
    source: design/gdd/point-cloud-renderer.md
    referenced_by:
      - design/gdd/point-cloud-renderer.md
      # Entity System GDD will reference these distance bands
    value: "d_min=0.0, d_max=3.0, J_max=0.180"
    unit: metres / metres-per-frame
    added: 2026-06-26
    revised: ""

  - name: T_session
    status: active
    source: design/gdd/floor-plan-system.md
    referenced_by:
      - design/gdd/floor-plan-system.md
      # Nominal session length; other timed systems (signal decay §15-D2,
      # Win/Lose) may reference
    value: 1500
    unit: seconds
    added: 2026-06-27
    revised: ""

  - name: corruption_threshold
    status: active
    source: design/gdd/scan-node-system.md
    referenced_by:
      - design/gdd/scan-node-system.md
      # Win/Lose GDD (when authored): integrity-failure ending (§11)
      # UI/HUD GDD (when authored): integrity warning display
    value: 4
    unit: invalid/corrupted captures
    added: 2026-06-27
    revised: ""

  - name: loop_trigger_tier
    status: active
    source: design/gdd/floor-plan-system.md
    referenced_by:
      - design/gdd/floor-plan-system.md
      # Keys off Entity/renderer proximity tiers (FAR/MEDIUM/NEAR/ADJACENT)
    value: "NEAR"
    unit: proximity tier
    added: 2026-06-27
    revised: ""

# Example:
#
# constants:
#   - name: gold_carry_limit
#     status: active
#     source: design/gdd/economy.md
#     referenced_by:
#       - design/gdd/economy.md
#       - design/gdd/inventory.md   # inventory enforces the carry limit
#       - design/gdd/ui.md          # HUD displays current gold vs limit
#     value: 9999
#     unit: gold
#     added: 2026-03-26
#     revised: ""
#
#   - name: base_inventory_slots
#     status: active
#     source: design/gdd/inventory.md
#     referenced_by:
#       - design/gdd/inventory.md
#       - design/gdd/progression.md  # progression unlocks additional slots
#     value: 20
#     unit: slots
#     added: 2026-03-26
#     revised: ""
